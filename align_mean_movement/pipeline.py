"""Pipeline orchestrator (reserved).

Currently, cli.main() contains the orchestrated pipeline to preserve identical behavior.
You can later migrate the body of cli.main() into this module without changing outputs.
"""
